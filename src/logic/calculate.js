// import Big from "big.js";

const calculate = (calcData, btnName) => {
  // const { total, next, operation } = calcData;

  if (btnName === '+/-') {
    window.console.log('multiply total and next by -1...');
  }
};

export default calculate;
